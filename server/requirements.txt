# snap server - Dipendenze di esercizio.
# remarks: Autore: Daniele Speziale - Data: 2026-08-26 - licenza MIT
Flask>=3.0,<4.0
Flask-WTF>=1.2,<2.0
cryptography>=42.0
pyotp>=2.9
qrcode[pil]>=8.0
tzdata>=2024.1
# Generazione dei report in PDF (licenza BSD, puro Python, nessuna libreria nativa).
# Concordata: vedi docs/08_REPORT.md, decisione D1.
reportlab>=4.0,<6.0
# Server WSGI di esercizio: lo prescrive lo stack del progetto ("in produzione l'app
# gira dietro Gunicorn, mai il dev server Flask"). Serve SOLO nei container Linux; in
# sviluppo su Windows si continua con il dev server avviato da start.ps1, perche'
# Gunicorn non gira su Windows (alternativa valutata per quel caso: waitress).
gunicorn>=22.0,<24.0
